
Session: BRAIN_OS + Read-Along App — major consolidation + system-truth pass.

DONE:
- MCP consolidation fully closed (Stage A + B). your_system learning path re-stitched
  (Kokoro af_heart), re-transcribed on GPU (2564 words), Drive file updated in-place
  (same ID, no orphan), backend transcript synced. Commits across both repos.
- Queue 2: 50 missing topics uploaded to Drive (Knowledge_OS/{domain}/sessions) and
  indexed by id:. drive_index now 114 entries. All 9 domains populated, karaoke live.
  Pilot-first then resumable batch, zero failures. (BRAIN_OS 6a600e8)
- LISTEN tab grouped UI shipped: Scheme C, 9-domain, color-coded CSS, deployed Vercel.
- Staging hygiene: deleted 26 clean {key}_audio twins (confirmed residue) + 1 dated
  session-archive audio (confirmed derivative, .md source intact). audio_staging clean
  for all unambiguous cases.
- CLAUDE.md rewritten + committed (7b8c9da): fixed BDF-centric framing to
  BRAIN_OS-as-graph + priority structure (construction=income), added how-I-work/learn
  + honesty clause, referenced Cristian_Principles.md (canonical, not duplicated).
  Merged carefully — preserved all operational rules (model standard, permissions,
  Telegram, MCP, session-close, hardware, .env).

DESIGNED, NOT BUILT (docs in 07_SYSTEM):
- Sessions/Project-Resumes tab — graph-shaped (resumes link into multiple contexts:
  Graph + the project they serve). Source = 07_SYSTEM/project docs via
  converter.py -> tts_local.py. Grouped by Projects + System/Graph. First build step:
  check if cross-context relations already live in vault [[backlinks]].

OPEN / NEXT:
- 3 staging orphans need individual decisions (cristianconstruction_audio,
  knowledge_ingestion_protocol_v2_audio [a v2 — maybe promote not delete],
  read_along_app_audio [maybe stale]). Details in handoff notes.
- read-along-app CLAUDE.md drift: lists deleted DRIVE_INDEX_JSON var + stale pipeline.
- Author + voice 3 topics: edge_tts, kokoro_tts, message_queues (need content written).
- Backend pipeline tools (gen_tts_staging, transcribe_batch, populate_staging,
  update_index) live in read-along-app/backend/ — found via dependency map.