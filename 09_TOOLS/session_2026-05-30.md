# Session Archive — 2026-05-30 — Karaoke + Knowledge OS + Voice Consistency
**Projects:** read-along-app, BRAIN_OS
**Date:** 2026-05-30

## Accomplished

### Karaoke completeness
- Built download_legacy.py — downloads legacy Drive audio by file ID for offline Whisper transcription
- Generated 25 transcript JSONs for all remaining legacy topics
- All 56 topics now have word-timestamp JSONs in backend/transcripts/ — full karaoke confirmed

### Knowledge OS tab — native vault browser
- Replaced static iframe with KnowledgeTab.tsx React component (3-view: folders, files, content)
- Backend: GET /vault/tree groups BRAIN_OS .md files by top-level folder (skips .obsidian, _archive, audio_staging)
- Backend: GET /vault/file fetches raw .md content from GitHub, 12k char truncation
- UX: folder list → filterable file list → rendered markdown with back navigation
- Markdown renderer handles headings, bold, italic, inline code, wiki-links, bullets, frontmatter strip

### Ask about document (Feature 1)
- Extended POST /ask: optional document_context field bypasses GitHub RAG entirely when present
- Claude receives only the document as context with a doc-focused system prompt
- KnowledgeTab content view: question input + streaming answer displayed below document

### Paragraph voice reading (Feature 2)
- Content split into blocks; blocks >= 5 lines or >= 280 chars get a floating sound button
- Tapping fetches /speak blob, plays via Audio object; re-tap stops; new file open stops audio
- Stop toggle: explicit playingIdx === idx check before fetching, prevents double-fetch race
- One paragraph plays at a time — new tap stops previous before starting

### Voice consistency
- /speak voice changed: en-US-GuyNeural to en-US-JennyNeural across all speak buttons
- ASK tab, KnowledgeTab paragraph reading, future Notes speak buttons all use same voice

### Bug fixes
- compile_session.py: git pull --rebase added before git commit — prevents push rejection when remote has unpulled commits
- backend send_telegram: strip whitespace from token + chat_id on env read
- backend send_telegram: validate chat_id as valid integer before sending (group IDs are negative)
- backend send_telegram: log full response body on non-200 for Railway log diagnosis

### Architecture locked in
- Sonnet 4.6 confirmed for all Claude API calls — never Opus (3x cost, not justified)
- CLAUDE.md model standard enforced: claude-sonnet-4-6 only

## Commits (read-along-app)
- 2231124 feat: add 25 legacy karaoke transcripts — all 56 topics have karaoke
- 4f05d21 feat: interactive vault browser for Knowledge OS tab
- 6f6fc07 feat: ask-doc + paragraph voice + fix ASK speak endpoint
- 0dd9552 fix: paragraph stop toggle + Telegram 400 + session rebase

## Commits (BRAIN_OS)
- 8598689 fix: git pull --rebase before push in compile_session.py

## Pending — Next Session
- Vercel auto-deploy: connect GitHub repo so push triggers deploy — eliminate manual npx vercel --prod
- Telegram 400: verify fix works on Railway after next restart
- Knowledge OS: live test vault browser on iPhone Safari
- Notes tab speak button: add per-note voice reading (uses same /speak + JennyNeural)

---
*Compiled by session_close.py — 2026-05-30*
