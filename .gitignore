# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
*.egg
.venv/
venv/
env/
ENV/
*.log
*.pot
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Environment
.env
.env.*
!.env.example

# Project-specific
Untitled.canvas
09_TOOLS/borrowed_audio_worklist.txt

# Claude Code   keep settings, ignore personal session data
.claude/
!.claude/settings.json

!.env.template

BrainOS_Book/audio/*.wav

03_APIS/.env
07_SYSTEM/financial logic.txt


audio_staging/*.mp3
audio_staging/*.wav

audio_staging/*.json

audio_staging/**/*.mp3
audio_staging/**/*.wav
audio_staging/**/*.json
audio_staging/_trash/
audio_staging/_paths/

# Python cache (never commit)
__pycache__/
*.pyc

# Obsidian session files (auto-rewritten, not knowledge)
.obsidian/graph.json
.obsidian/workspace.json
Untitled*.canvas
Untitled*.base
*.base
202[67]-??-??.md

.claude/settings.local.json

# Generated graph artifacts (regenerable via 09_TOOLS/brain_graph.py — never commit)
02_PROJECTS/graphs/brain_os_*.html
02_PROJECTS/graphs/brain_os_*.json

# Session narration staged for Kokoro TTS — derived from 08_SESSIONS/*.md (never commit)
audio_staging/*.txt
audio_staging/**/*.txt

# Obsidian MCP transaction/lock state (local bridge bookkeeping, not vault content)
.obsidian-mcp/
# Per-call Claude API usage, written by claude_client._log_usage.
# Append-only local spend record, grows forever, no value in history.
05_MEMORY/claude_usage.jsonl
